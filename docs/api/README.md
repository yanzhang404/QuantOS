# API

The future Go API will expose project, dataset, strategy, backtest, experiment,
and task resources. Contracts must be versioned, observable, and independent of
module-internal persistence models.
